<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>REJA</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css"
        integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
</head>
<style>
    .bg-video {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        object-fit: cover;
        z-index: -2;
    }

    body::before {
        content: "";
        position: fixed;
        inset: 0;
        background: rgba(0, 0, 0, 0.4);
        z-index: -1;
    }

    body {
        position: relative;
        color: #fff;
    }
</style>

<body>
    <video autoplay muted loop playsinline class="bg-video">
        <source src="/videos/car.mp4" type="video/mp4">
    </video>

    <div class="container text-center">
        <h1 class="display-4 py-2" style="color: rgb(240, 15, 34); font-family: Automatic;">REJA VA MAQSADLAR</h1>

        <form id="create-form" action="/create-item" method="POST" class="d-flex align-items-center mx-auto shadow-lg"
            style="max-width:800px;padding:14px;border-radius:16px;
           background:rgba(0,0,0,.45);backdrop-filter:blur(10px)">

            <input id="create-field" name="reja" autofocus autocomplete="off" class="form-control me-3"
                style="background:rgba(255,255,255,.15);color:#fff;border:none" placeholder="Yangi reja kiriting...">

            <button class="btn btn-outline-success" style="border-radius: 20px">
                Yangi Reja Qo'shish!
            </button>
        </form>
    </div>




    <ul id="item-list" class="list-group pb-4 mx-auto" style="max-width:800px">
        <% items.map(item=> { %>
            <li class="list-group-item d-flex justify-content-between align-items-center shadow-lg" style="background:rgba(255,255,255,.15);border:none;
             backdrop-filter:blur(10px);border-radius:16px;color:#fff">

                <span class="item-text">
                    <%= item.reja %>
                </span>

                <div>
                    <button data-id="<%=item._id%>" class="edit-me btn btn-outline-success btn-sm me-2"
                        style="border-radius:12px">O'zgartirish</button>
                    <button data-id="<%=item._id%>" class="delete-me btn btn-outline-danger btn-sm"
                        style="border-radius:12px">O'chirish</button>
                </div>
            </li>
            <% }) %>
    </ul>

    <div class="text-center">
        <button id="clean-all" class="btn btn-outline-danger shadow-lg"
            style="border-radius:16px;background:rgba(0,0,0,.45);backdrop-filter:blur(10px)">
            Hamma rejalarni o'chirish
        </button>
    </div>

    </div>


    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
    <script src="/browser.js"></script>
</body>

</html>